services:
  openclaw:
    image: m.daocloud.io/ghcr.io/openclaw/openclaw:latest
    container_name: openclaw
    ports:
      - "127.0.0.1:18789:18789"
    volumes:
      - .openclaw:/home/node/.openclaw
    command: openclaw gateway --allow-unconfigured --bind lan
    restart: unless-stopped
